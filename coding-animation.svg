<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 370" width="900" height="370">
  <!-- DEFS: 8 gradients + 3 filters + 1 clipPath (lines 2-55) -->
  <!-- STYLE: 20 @keyframes + 24 CSS classes (lines 57-104) -->
  <!-- BACKGROUND + 16 STARS (lines 106-117) -->
  <!-- MATRIX RAIN: 8 columns (lines 119-129) -->
  <!-- AMBIENT GLOW (lines 131-133) -->
  <!-- DESK + NEON UNDERGLOW (lines 135-144) -->
  <!-- MONITOR (lines 146-162) -->
  <!-- CODE ON SCREEN: 16s looping (lines 164-238) -->
  <!-- PLANT (lines 240-250) -->
  <!-- DEVELOPER: chair, body, arms, head, glasses, headphones, EQ bars (lines 252-329) -->
  <!-- RGB KEYBOARD: 4 rows + color cycling (lines 331-366) -->
  <!-- MOUSE + RGB strip (lines 368-375) -->
  <!-- COFFEE MUG + steam (lines 377-389) -->
  <!-- SLEEPING CAT + zzz (lines 391-409) -->
  <!-- 9 FLOATING CODE SYMBOLS (lines 411-420) -->
  <!-- BOTTOM FADE + TAGLINE (lines 422-428) -->
</svg>
